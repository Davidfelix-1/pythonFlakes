import array

one_dime = array.array('i', [x for x in range(2, 21, 2)])
print(one_dime)

# This array prints out the range of number in 21 , starting from two and increased by two
# it produces the ven integers from 2 to 20
